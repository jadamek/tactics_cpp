#include <iostream>
#include <SFML/Graphics.hpp>
#include <vector>
#include <set>
#include <list>


class Obj
{
public:
    bool whatever;
    int id;
};

int main()
{
    sf::Clock timer;
    float elapsed;
    
    std::vector<Obj*> objs;
    for(int i = 0; i < 10000; i++)
    {
        objs.push_back(new Obj);
    }    
    std::vector<Obj*> other;
    timer.restart();
    for(int i = 0; i < 10000; i++)
    {
        other.push_back(objs[i]);
    }
    elapsed = timer.restart().asMicroseconds();

    std::cout << "Execution time: " << elapsed << " us " << std::endl;

    return 0;
}