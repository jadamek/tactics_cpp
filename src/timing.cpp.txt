#include <iostream>
#include <SFML/Graphics.hpp>
#include <vector>
#include <set>
#include <list>
#include "game/ManhattanArea.h"
#include "control/TargetConfirmer.h"

int main()
{
    sf::Clock timer;
    float elapsed;
    
    
    timer.restart();
    for(int i = 0; i < 1; i++)
    {
        ManhattanArea::compute(10, sf::Vector2f(), [](const sf::Vector2f&, const sf::Vector2f&){return true;});
    }
    elapsed = timer.restart().asMicroseconds();

    std::cout << "Execution time (lamb): " << elapsed << " us " << std::endl;

    return 0;
}